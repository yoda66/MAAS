stages:
    - BakeMalware
    - PostProcess

workflow:
    rules:
        - changes:
            - shellcode/*

ScareCrow:
    stage: BakeMalware
    tags:
        - maas
    script:
        - |
            ScareCrow -I $CI_PROJECT_DIR/shellcode/notepad_x64.bin -Loader binary -nosign
            ls -l /runner
            ls -l $CI_PROJECT_DIR
            
ZipFiles:
    stage: PostProcess
    tags:
        - maas
    script:
        - |
            echo "Creating ZIP file"
